package com.coretek.avt.executor.util;

import com.coretek.avt.executor.model.Message;

/**
 * 消息编码器
 * @author David
 *
 */
public class MessageEncoder
{
	/**
	 * 对消息进行编码
	 * @param msg
	 * @param periodIndex 需要被编码的周期数，从1开始。
	 * @return
	 */
	public static byte[] Encode(Message msg, int periodIndex)
	{
		byte[] data = new byte[2048];
		
		return data;
	}
}